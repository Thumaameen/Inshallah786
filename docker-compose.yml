version: '3.8'

services:
  app:
    build: .
    ports:
      - "10000:10000"
    environment:
      - NODE_ENV=production
      - PORT=10000
      - HOST=0.0.0.0
    volumes:
      - ./dist:/app/dist
      - ./node_modules:/app/node_modules
    restart: unless-stopped